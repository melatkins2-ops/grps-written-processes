GRPS Written Process Collaborative - single-file deployment

Upload ONLY index.html to the ROOT of the existing GitHub repository, replacing the old index.html. Commit directly to main. GitHub Pages: Settings > Pages > Deploy from a branch > main > / (root). This version embeds the GRPS logo, all 54 DOCX process documents, the master XLSX index, and all four meeting templates. No documents, templates, assets, CSS, JS or JSON folders are needed. Remove old files if desired after successful verification.
